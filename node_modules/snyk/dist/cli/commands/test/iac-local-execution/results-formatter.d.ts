import { IacFileScanResult, PolicyMetadata } from './types';
import { SEVERITY } from '../../../../lib/snyk-test/common';
import { IacProjectType } from '../../../../lib/iac/constants';
export declare function formatResults(iacLocalExecutionResults: Array<IacFileScanResult>, options: {
    severityThreshold?: SEVERITY;
}): {
    result: {
        cloudConfigResults: PolicyMetadata[];
    };
    isPrivate: boolean;
    packageManager: IacProjectType;
    targetFile: string;
}[];
